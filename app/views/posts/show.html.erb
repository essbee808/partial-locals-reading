Information About the Post
<%= render partial: "authors/author", locals: {post_author: @author} %>
<!-- first key-value pair tells Rails the name of the partial to render ("authors/author")
second key-value pair specifies the locals as a Hash; hash's keys (post_author) will be
created as local variables within the partial -->

<%= @post.title %>
<%= @post.content %>
